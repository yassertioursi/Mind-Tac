import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color(0xFF1B5E20);     // Dark green
  static const secondaryColor = Color(0xFF2E7D32);   // Medium green
  static const accentColor = Color(0xFF81C784);      // Light green
  static const tileColor = Color(0xCCB2DFDB);        // Glass-like
  static const xColor = Color(0xFF00E676);           // Neon green for X
  static const oColor = Color(0xFF69F0AE);           // Lighter neon green for O
  static const shadow = Colors.black45;
}
